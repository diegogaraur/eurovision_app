import 'package:flutter/material.dart';

class TopsView extends StatelessWidget {
  const TopsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Tops'),
    );
  }
}
