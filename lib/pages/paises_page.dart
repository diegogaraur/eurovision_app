import 'package:flutter/material.dart';

class PaisesPage extends StatelessWidget {
  const PaisesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('PaisesPage'),
      ),
    );
  }
}
